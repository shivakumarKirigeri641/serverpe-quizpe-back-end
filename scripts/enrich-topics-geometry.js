/**
 * scripts/enrich-topics-geometry.js
 * ---------------------------------------------------------------------------
 * Question shapes for geometry, mensuration, coordinate geometry, trigonometry,
 * statistics and probability.
 *
 * Geometry on WhatsApp cannot rely on a diagram, so every question here is
 * written to stand on its own in words. Where a figure would normally carry the
 * information ("in the figure, AB ∥ CD"), the text states it instead.
 * ---------------------------------------------------------------------------
 */

const H = require('./enrich-questions.js');
const { ri, pick, opts, yesNo, among, near, fmt, round2, NAMES } = H;

const PI = 22 / 7;

/**
 * Angle answers carry their degree sign — a bare 76 is not an angle. Negative
 * and zero distractors are dropped: no angle in these questions can be either,
 * so they would be free eliminations.
 */
const optsDeg = (v, ds) => opts(v + '°',
  (ds || []).filter((d) => Number(d) > 0).map((d) => d + '°'));

/* ======================================================== lines and angles === */
const linesAngles = [
  () => { const a = ri(10, 80); return { wa: `📐 Two angles are complementary. One is ${a}°. What is the other?`, o: optsDeg(90 - a, near(90 - a)), exp: `Complementary angles add to 90°, so the other is 90° − ${a}° = ${90 - a}°.` }; },
  () => { const a = ri(20, 160); return { wa: `📐 Two angles are supplementary. One is ${a}°. What is the other?`, o: optsDeg(180 - a, near(180 - a)), exp: `Supplementary angles add to 180°, so the other is 180° − ${a}° = ${180 - a}°.` }; },
  () => { const a = ri(20, 160); return { wa: `✖️ Two lines cross. One angle is ${a}°. What is the angle vertically opposite it?`, o: optsDeg(a, [180 - a, 90 - a, a + 10]), exp: `Vertically opposite angles are equal, so it is also ${a}°.` }; },
  () => { const a = ri(20, 160); return { wa: `🛤️ A line crosses two parallel lines. One angle is ${a}°. What is its co-interior (allied) angle?`, o: optsDeg(180 - a, [a, 90 - a, 360 - a]), exp: `Co-interior angles between parallel lines add to 180°, so it is ${180 - a}°.` }; },
  () => { const a = ri(20, 160); return { wa: `🛤️ A line crosses two parallel lines. One angle is ${a}°. What is the corresponding angle?`, o: optsDeg(a, [180 - a, 90 - a, Math.round(a / 2)]), exp: `Corresponding angles between parallel lines are equal, so it is ${a}°.` }; },
  () => { const a = ri(30, 120), b = ri(20, 170 - a); return { wa: `📏 Three angles on a straight line are ${a}°, ${b}° and x°. Find x.`, o: optsDeg(180 - a - b, near(180 - a - b)), exp: `Angles on a straight line add to 180°: x = 180 − ${a} − ${b} = ${180 - a - b}°.` }; },
  () => { const a = ri(50, 150), b = ri(40, 340 - a), c = ri(20, 350 - a - b); return { wa: `🎯 Four angles at a point are ${a}°, ${b}°, ${c}° and x°. Find x.`, o: optsDeg(360 - a - b - c, near(360 - a - b - c)), exp: `Angles at a point add to 360°: x = 360 − ${a} − ${b} − ${c} = ${360 - a - b - c}°.` }; },
  () => { const a = pick([[45, 'acute'], [90, 'right'], [120, 'obtuse'], [180, 'straight'], [30, 'acute'], [100, 'obtuse'], [270, 'reflex']]); return { wa: `🏷️ What kind of angle measures ${a[0]}°?`, o: among(a[1], ['acute', 'right', 'obtuse', 'straight', 'reflex'].filter((x) => x !== a[1]).slice(0, 3)), exp: `${a[0]}° is ${a[1]}${a[1] === 'acute' ? ' — less than 90°' : a[1] === 'obtuse' ? ' — between 90° and 180°' : a[1] === 'reflex' ? ' — more than 180°' : ''}.` }; },
  () => { const a = ri(20, 70); return { wa: `🪞 An angle is ${a}°. What is its supplement minus its complement?`, o: optsDeg(90, [180 - 2 * a, a, 2 * a]), exp: `Supplement = ${180 - a}°, complement = ${90 - a}°. Difference = ${180 - a} − ${90 - a} = 90° — always 90°.` }; },
  () => { const k = ri(2, 5), a = 180 / (k + 1); return { wa: `⚖️ Two supplementary angles are in the ratio 1 : ${k}. What is the SMALLER angle?`, o: optsDeg(round2(a), near(round2(a))), exp: `The parts total ${k + 1}, so one part = 180 ÷ ${k + 1} = ${round2(a)}°.` }; },
];

/* ============================================================== triangles === */
const triangles = [
  () => { const a = ri(30, 90), b = ri(20, 150 - a); return { wa: `🔺 Two angles of a triangle are ${a}° and ${b}°. Find the third.`, o: optsDeg(180 - a - b, near(180 - a - b)), exp: `The three angles add to 180°: 180 − ${a} − ${b} = ${180 - a - b}°.` }; },
  () => { const a = ri(20, 80); return { wa: `🔺 In a right-angled triangle one acute angle is ${a}°. What is the other acute angle?`, o: optsDeg(90 - a, near(90 - a)), exp: `The two acute angles add to 90°, so the other is 90° − ${a}° = ${90 - a}°.` }; },
  () => { const a = ri(20, 80); return { wa: `🔺 An isosceles triangle has its apex angle ${a}°. What is each base angle?`, o: optsDeg(round2((180 - a) / 2), near(round2((180 - a) / 2))), exp: `The two base angles are equal and share 180° − ${a}° = ${180 - a}°, so each is ${round2((180 - a) / 2)}°.` }; },
  () => { const t = pick([[3, 4, 5], [6, 8, 10], [5, 12, 13], [8, 15, 17], [9, 12, 15], [7, 24, 25]]); return { wa: `📐 A right-angled triangle has legs ${t[0]} cm and ${t[1]} cm. Find the hypotenuse.`, o: opts(t[2], near(t[2])), exp: `By Pythagoras: ${t[0]}² + ${t[1]}² = ${t[0] ** 2} + ${t[1] ** 2} = ${t[2] ** 2}, so the hypotenuse is √${t[2] ** 2} = ${t[2]} cm.` }; },
  () => { const t = pick([[3, 4, 5], [6, 8, 10], [5, 12, 13], [8, 15, 17], [9, 12, 15]]); return { wa: `📐 A right-angled triangle has hypotenuse ${t[2]} cm and one leg ${t[0]} cm. Find the other leg.`, o: opts(t[1], near(t[1])), exp: `${t[2]}² − ${t[0]}² = ${t[2] ** 2} − ${t[0] ** 2} = ${t[1] ** 2}, so the other leg is ${t[1]} cm.` }; },
  () => { const b = ri(4, 30), h = ri(4, 30); return { wa: `📏 Find the area of a triangle with base ${b} cm and height ${h} cm.`, o: opts(round2((b * h) / 2) + ' cm²', [b * h + ' cm²', round2((b + h) / 2) + ' cm²', round2((b * h) / 2 + 1) + ' cm²']), exp: `Area = ½ × base × height = ½ × ${b} × ${h} = ${round2((b * h) / 2)} cm².` }; },
  () => { const a = ri(4, 20), b = ri(4, 20), c = ri(Math.abs(a - b) + 1, a + b - 1); return { wa: `📐 Find the perimeter of a triangle with sides ${a} cm, ${b} cm and ${c} cm.`, o: opts(a + b + c + ' cm', [a * b * c + ' cm', round2((a + b + c) / 2) + ' cm', a + b + c + 1 + ' cm']), exp: `Perimeter = ${a} + ${b} + ${c} = ${a + b + c} cm.` }; },
  () => { const a = ri(3, 12), b = ri(3, 12); const ok = pick([true, false]); const c = ok ? ri(Math.abs(a - b) + 1, a + b - 1) : a + b + ri(1, 5); return { wa: `🔍 Can a triangle have sides ${a} cm, ${b} cm and ${c} cm?`, o: yesNo(ok, 'Yes', 'No'), exp: `Any two sides must add to more than the third. ${a} + ${b} = ${a + b}, which is ${ok ? `greater than ${c} — so yes` : `not greater than ${c} — so no`}.` }; },
  () => { const a = ri(30, 80), b = ri(30, 150 - a); const ext = a + b; return { wa: `📐 In a triangle, the two interior angles opposite an exterior angle are ${a}° and ${b}°. Find the exterior angle.`, o: optsDeg(ext, near(ext)), exp: `An exterior angle equals the sum of the two opposite interior angles: ${a} + ${b} = ${ext}°.` }; },
  () => { const s = pick([['all three sides equal', 'Equilateral'], ['exactly two sides equal', 'Isosceles'], ['no two sides equal', 'Scalene']]); return { wa: `🏷️ What is a triangle with ${s[0]} called?`, o: among(s[1], ['Equilateral', 'Isosceles', 'Scalene', 'Right-angled'].filter((x) => x !== s[1]).slice(0, 3)), exp: `A triangle with ${s[0]} is ${s[1].toLowerCase()}.` }; },
  () => { return { wa: `🔺 Each angle of an equilateral triangle measures?`, o: opts('60°', ['90°', '45°', '180°']), exp: `All three angles are equal and add to 180°, so each is 180 ÷ 3 = 60°.` }; },
  () => { const a = ri(2, 6), k = ri(2, 4); return { wa: `🔁 Two triangles are similar. The first has a side ${a} cm matching a side ${a * k} cm in the second. If another side of the first is ${a + 2} cm, what is the matching side?`, o: opts((a + 2) * k, near((a + 2) * k)), exp: `The scale factor is ${a * k} ÷ ${a} = ${k}, so the matching side is ${a + 2} × ${k} = ${(a + 2) * k} cm.` }; },
  () => { const t = pick([[3, 4, 5], [6, 8, 10], [5, 12, 13]]); return { wa: `📐 Is a triangle with sides ${t[0]}, ${t[1]} and ${t[2]} right-angled?`, o: yesNo(true, 'Yes', 'No'), exp: `${t[0]}² + ${t[1]}² = ${t[0] ** 2 + t[1] ** 2} = ${t[2]}², so it satisfies Pythagoras — yes.` }; },
];

/* ========================================================= quadrilaterals === */
const quadrilaterals = [
  () => { const a = ri(50, 120), b = ri(50, 120), c = ri(40, 340 - a - b); return { wa: `⬛ Three angles of a quadrilateral are ${a}°, ${b}° and ${c}°. Find the fourth.`, o: optsDeg(360 - a - b - c, near(360 - a - b - c)), exp: `The four angles add to 360°: 360 − ${a} − ${b} − ${c} = ${360 - a - b - c}°.` }; },
  () => { const a = ri(40, 140); return { wa: `▱ One angle of a parallelogram is ${a}°. What is the angle NEXT to it?`, o: optsDeg(180 - a, [a, 90, 360 - a]), exp: `Adjacent angles of a parallelogram add to 180°, so it is ${180 - a}°.` }; },
  () => { const a = ri(40, 140); return { wa: `▱ One angle of a parallelogram is ${a}°. What is the angle OPPOSITE it?`, o: optsDeg(a, [180 - a, 90, 360 - a]), exp: `Opposite angles of a parallelogram are equal, so it is ${a}°.` }; },
  () => { const l = ri(4, 25), w = ri(3, 20); return { wa: `⬜ Find the area of a rectangle ${l} cm long and ${w} cm wide.`, o: opts(l * w + ' cm²', [2 * (l + w) + ' cm²', l + w + ' cm²', l * w + 1 + ' cm²']), exp: `Area = length × width = ${l} × ${w} = ${l * w} cm².` }; },
  () => { const l = ri(4, 25), w = ri(3, 20); return { wa: `⬜ Find the perimeter of a rectangle ${l} cm long and ${w} cm wide.`, o: opts(2 * (l + w) + ' cm', [l * w + ' cm', l + w + ' cm', 2 * l + w + ' cm']), exp: `Perimeter = 2 × (${l} + ${w}) = ${2 * (l + w)} cm.` }; },
  () => { const s = ri(3, 25); return { wa: `⬜ Find the area of a square of side ${s} cm.`, o: opts(s * s + ' cm²', [4 * s + ' cm²', 2 * s + ' cm²', s * s + 1 + ' cm²']), exp: `Area = side × side = ${s} × ${s} = ${s * s} cm².` }; },
  () => { const b = ri(4, 20), h = ri(3, 18); return { wa: `▱ Find the area of a parallelogram with base ${b} cm and height ${h} cm.`, o: opts(b * h + ' cm²', [round2((b * h) / 2) + ' cm²', 2 * (b + h) + ' cm²', b + h + ' cm²']), exp: `Area = base × height = ${b} × ${h} = ${b * h} cm².` }; },
  () => { const d1 = ri(4, 20), d2 = ri(4, 20); return { wa: `🔷 Find the area of a rhombus whose diagonals are ${d1} cm and ${d2} cm.`, o: opts(round2((d1 * d2) / 2) + ' cm²', [d1 * d2 + ' cm²', round2((d1 + d2) / 2) + ' cm²', round2((d1 * d2) / 4) + ' cm²']), exp: `Area = ½ × d₁ × d₂ = ½ × ${d1} × ${d2} = ${round2((d1 * d2) / 2)} cm².` }; },
  () => { const a = ri(4, 20), b = ri(4, 20), h = ri(3, 15); return { wa: `⏢ Find the area of a trapezium with parallel sides ${a} cm and ${b} cm, height ${h} cm.`, o: opts(round2(((a + b) * h) / 2) + ' cm²', [(a + b) * h + ' cm²', round2((a * b * h) / 2) + ' cm²', a + b + h + ' cm²']), exp: `Area = ½ × (${a} + ${b}) × ${h} = ½ × ${a + b} × ${h} = ${round2(((a + b) * h) / 2)} cm².` }; },
  () => { const q = pick([['a square', 'All four sides equal and all angles 90°'], ['a rhombus', 'All four sides equal but angles need not be 90°'], ['a rectangle', 'Opposite sides equal and all angles 90°']]); return { wa: `🏷️ Which statement describes ${q[0]}?`, o: among(q[1], ['All four sides equal and all angles 90°', 'All four sides equal but angles need not be 90°', 'Opposite sides equal and all angles 90°', 'Only one pair of sides is parallel'].filter((x) => x !== q[1]).slice(0, 3)), exp: `${q[0].charAt(0).toUpperCase() + q[0].slice(1)}: ${q[1].toLowerCase()}.` }; },
  () => { const s = ri(4, 20); return { wa: `⬜ The perimeter of a square is ${4 * s} cm. What is its side?`, o: opts(s + ' cm', [4 * s + ' cm', round2(s / 2) + ' cm', 2 * s + ' cm']), exp: `A square has 4 equal sides: ${4 * s} ÷ 4 = ${s} cm.` }; },
  () => { const l = ri(5, 20), a = l * ri(3, 15); return { wa: `⬜ A rectangle has area ${a} cm² and length ${l} cm. Find its width.`, o: opts(a / l + ' cm', [a * l + ' cm', round2(a / l + 1) + ' cm', l + ' cm']), exp: `Width = area ÷ length = ${a} ÷ ${l} = ${a / l} cm.` }; },
];

/* ================================================================= circles === */
const circles = [
  () => { const r = pick([7, 14, 21, 28, 35]); return { wa: `⭕ Find the circumference of a circle of radius ${r} cm. (Use π = 22/7)`, o: opts(round2(2 * PI * r) + ' cm', [round2(PI * r * r) + ' cm', round2(PI * r) + ' cm', round2(2 * PI * r + 2) + ' cm']), exp: `Circumference = 2πr = 2 × 22/7 × ${r} = ${round2(2 * PI * r)} cm.` }; },
  () => { const r = pick([7, 14, 21, 28]); return { wa: `⭕ Find the area of a circle of radius ${r} cm. (Use π = 22/7)`, o: opts(round2(PI * r * r) + ' cm²', [round2(2 * PI * r) + ' cm²', round2(PI * r) + ' cm²', round2(PI * r * r + 4) + ' cm²']), exp: `Area = πr² = 22/7 × ${r}² = 22/7 × ${r * r} = ${round2(PI * r * r)} cm².` }; },
  () => { const r = ri(3, 30); return { wa: `⭕ A circle has radius ${r} cm. What is its diameter?`, o: opts(2 * r + ' cm', [round2(r / 2) + ' cm', r + ' cm', 4 * r + ' cm']), exp: `Diameter = 2 × radius = 2 × ${r} = ${2 * r} cm.` }; },
  () => { const d = ri(4, 40) * 2; return { wa: `⭕ A circle has diameter ${d} cm. What is its radius?`, o: opts(d / 2 + ' cm', [2 * d + ' cm', d + ' cm', round2(d / 4) + ' cm']), exp: `Radius = diameter ÷ 2 = ${d} ÷ 2 = ${d / 2} cm.` }; },
  () => { const r = pick([7, 14, 21]); const ang = pick([90, 180, 60, 120]); const a = (ang / 360) * PI * r * r; return { wa: `🥧 Find the area of a sector of angle ${ang}° in a circle of radius ${r} cm. (π = 22/7)`, o: opts(round2(a) + ' cm²', [round2(PI * r * r) + ' cm²', round2(a * 2) + ' cm²', round2(a + 5) + ' cm²']), exp: `Sector area = (${ang}/360) × πr² = (${ang}/360) × ${round2(PI * r * r)} = ${round2(a)} cm².` }; },
  () => { const r = pick([7, 14, 21]); const ang = pick([90, 180, 60, 120]); const l = (ang / 360) * 2 * PI * r; return { wa: `🥧 Find the length of an arc of angle ${ang}° in a circle of radius ${r} cm. (π = 22/7)`, o: opts(round2(l) + ' cm', [round2(2 * PI * r) + ' cm', round2(l * 2) + ' cm', round2(l + 3) + ' cm']), exp: `Arc length = (${ang}/360) × 2πr = (${ang}/360) × ${round2(2 * PI * r)} = ${round2(l)} cm.` }; },
  () => { return { wa: `📐 What is the angle in a semicircle?`, o: opts('90°', ['180°', '60°', '45°']), exp: `The angle subtended by a diameter at any point on the circle is always a right angle, 90°.` }; },
  () => { const a = ri(20, 80); return { wa: `⭕ An arc subtends ${a}° at the centre of a circle. What angle does it subtend at a point on the remaining circle?`, o: opts(round2(a / 2) + '°', [a + '°', 2 * a + '°', 180 - a + '°']), exp: `The angle at the centre is twice the angle at the circumference: ${a} ÷ 2 = ${round2(a / 2)}°.` }; },
  () => { const a = ri(40, 140); return { wa: `⭕ In a cyclic quadrilateral, one angle is ${a}°. What is the angle opposite it?`, o: optsDeg(180 - a, [a, 90, 360 - a]), exp: `Opposite angles of a cyclic quadrilateral add to 180°, so it is ${180 - a}°.` }; },
  () => { const r = ri(5, 20), d = ri(21, 40); return { wa: `📏 A tangent touches a circle of radius ${r} cm. What angle does it make with the radius at the point of contact?`, o: opts('90°', ['45°', '180°', '60°']), exp: `A tangent is always perpendicular to the radius at the point where it touches — 90°.` }; },
  () => { const c = pick([['radius', 'the distance from the centre to the edge'], ['chord', 'a line joining two points on the circle'], ['diameter', 'a chord passing through the centre'], ['tangent', 'a line touching the circle at exactly one point']]); return { wa: `🏷️ What is a ${c[0]} of a circle?`, o: among(c[1], ['the distance from the centre to the edge', 'a line joining two points on the circle', 'a chord passing through the centre', 'a line touching the circle at exactly one point'].filter((x) => x !== c[1]).slice(0, 3)), exp: `A ${c[0]} is ${c[1]}.` }; },
];

/* ======================================================= perimeter and area === */
const perimeterArea = [
  () => { const l = ri(4, 30), w = ri(3, 25); return { wa: `⬜ A garden is ${l} m long and ${w} m wide. What is its area?`, o: opts(l * w + ' m²', [2 * (l + w) + ' m²', l + w + ' m²', l * w + 5 + ' m²']), exp: `Area = ${l} × ${w} = ${l * w} m².` }; },
  () => { const l = ri(4, 30), w = ri(3, 25); return { wa: `🚧 How much fencing is needed around a field ${l} m by ${w} m?`, o: opts(2 * (l + w) + ' m', [l * w + ' m', l + w + ' m', 2 * l + ' m']), exp: `Fencing follows the perimeter: 2 × (${l} + ${w}) = ${2 * (l + w)} m.` }; },
  () => { const s = ri(3, 25); return { wa: `⬜ A square field has side ${s} m. What is its perimeter?`, o: opts(4 * s + ' m', [s * s + ' m', 2 * s + ' m', 4 * s + 2 + ' m']), exp: `Perimeter = 4 × ${s} = ${4 * s} m.` }; },
  () => { const b = ri(5, 25), h = ri(4, 20); return { wa: `🔺 Find the area of a triangular plot with base ${b} m and height ${h} m.`, o: opts(round2((b * h) / 2) + ' m²', [b * h + ' m²', b + h + ' m²', round2((b * h) / 2 + 3) + ' m²']), exp: `Area = ½ × ${b} × ${h} = ${round2((b * h) / 2)} m².` }; },
  // The border is sized from the SHORTER side, so the inner rectangle always
  // has room to exist — a 3 cm border on a 4 cm photo leaves nothing.
  () => { const w = ri(8, 16), l = w + ri(2, 10); const t = ri(1, Math.floor((w - 2) / 2)); const inner = (l - 2 * t) * (w - 2 * t); return { wa: `🖼️ A photo ${l} cm by ${w} cm has a ${t} cm border all around inside it. What is the area of the inner part?`, o: opts(inner + ' cm²', [l * w + ' cm²', (l - t) * (w - t) + ' cm²', inner + 4 + ' cm²']), exp: `The border takes ${t} cm off each edge, so the inner part is (${l} − ${2 * t}) × (${w} − ${2 * t}) = ${l - 2 * t} × ${w - 2 * t} = ${inner} cm².` }; },
  () => { const s = ri(4, 20); return { wa: `🔁 The area of a square is ${s * s} cm². What is its side?`, o: opts(s + ' cm', [s * s + ' cm', 4 * s + ' cm', round2(s / 2) + ' cm']), exp: `Side = √${s * s} = ${s} cm.` }; },
  () => { const l = ri(5, 20), w = ri(4, 15), k = 2; return { wa: `📈 A rectangle is ${l} cm by ${w} cm. If both sides are doubled, how many times bigger is the area?`, o: opts('4 times', ['2 times', '8 times', 'the same']), exp: `New area = ${2 * l} × ${2 * w} = ${4 * l * w} cm², and ${4 * l * w} ÷ ${l * w} = 4 times bigger.` }; },
  () => { const tiles = ri(4, 20), side = ri(2, 6); return { wa: `🧱 A floor is covered by ${tiles * tiles} square tiles of side ${side} cm each. What is the floor's area?`, o: opts(tiles * tiles * side * side + ' cm²', [tiles * side + ' cm²', tiles * tiles * side + ' cm²', tiles * side * side + ' cm²']), exp: `Each tile is ${side * side} cm², so ${tiles * tiles} tiles cover ${tiles * tiles} × ${side * side} = ${fmt(tiles * tiles * side * side)} cm².` }; },
  () => { const a = ri(4, 20), b = ri(4, 20), c = ri(Math.abs(a - b) + 1, a + b - 1); return { wa: `🔺 A triangular park has sides ${a} m, ${b} m and ${c} m. How far is one full round?`, o: opts(a + b + c + ' m', [a * b * c + ' m', round2((a + b + c) / 2) + ' m', a + b + ' m']), exp: `One round is the perimeter: ${a} + ${b} + ${c} = ${a + b + c} m.` }; },
  () => { const l = ri(6, 25), w = ri(4, 20); return { wa: `🔎 A rectangle and a square both have perimeter ${2 * (l + w)} cm. What is the square's side?`, o: opts(round2((2 * (l + w)) / 4) + ' cm', [l + ' cm', w + ' cm', round2((l + w) / 2 + 1) + ' cm']), exp: `A square's side = perimeter ÷ 4 = ${2 * (l + w)} ÷ 4 = ${round2((2 * (l + w)) / 4)} cm.` }; },
];

/* =============================================== surface area and volume === */
const solids = [
  () => { const s = ri(2, 15); return { wa: `🧊 Find the volume of a cube of edge ${s} cm.`, o: opts(s ** 3 + ' cm³', [6 * s * s + ' cm³', s * s + ' cm³', 3 * s + ' cm³']), exp: `Volume = edge³ = ${s}³ = ${fmt(s ** 3)} cm³.` }; },
  () => { const s = ri(2, 15); return { wa: `🧊 Find the total surface area of a cube of edge ${s} cm.`, o: opts(6 * s * s + ' cm²', [s ** 3 + ' cm²', 4 * s * s + ' cm²', s * s + ' cm²']), exp: `A cube has 6 equal faces: 6 × ${s}² = 6 × ${s * s} = ${fmt(6 * s * s)} cm².` }; },
  () => { const l = ri(3, 15), b = ri(2, 12), h = ri(2, 12); return { wa: `📦 Find the volume of a cuboid ${l} cm × ${b} cm × ${h} cm.`, o: opts(l * b * h + ' cm³', [2 * (l * b + b * h + h * l) + ' cm³', l + b + h + ' cm³', l * b + ' cm³']), exp: `Volume = ${l} × ${b} × ${h} = ${fmt(l * b * h)} cm³.` }; },
  () => { const l = ri(3, 12), b = ri(2, 10), h = ri(2, 10); const sa = 2 * (l * b + b * h + h * l); return { wa: `📦 Find the total surface area of a cuboid ${l} cm × ${b} cm × ${h} cm.`, o: opts(sa + ' cm²', [l * b * h + ' cm²', round2(sa / 2) + ' cm²', sa + 6 + ' cm²']), exp: `TSA = 2(lb + bh + hl) = 2(${l * b} + ${b * h} + ${h * l}) = ${fmt(sa)} cm².` }; },
  () => { const r = pick([7, 14, 21]), h = ri(5, 20); const v = PI * r * r * h; return { wa: `🥫 Find the volume of a cylinder of radius ${r} cm and height ${h} cm. (π = 22/7)`, o: opts(round2(v) + ' cm³', [round2(2 * PI * r * h) + ' cm³', round2(PI * r * h) + ' cm³', round2(v + 20) + ' cm³']), exp: `Volume = πr²h = 22/7 × ${r * r} × ${h} = ${fmt(round2(v))} cm³.` }; },
  () => { const r = pick([7, 14, 21]), h = ri(5, 20); const csa = 2 * PI * r * h; return { wa: `🥫 Find the curved surface area of a cylinder of radius ${r} cm and height ${h} cm. (π = 22/7)`, o: opts(round2(csa) + ' cm²', [round2(PI * r * r * h) + ' cm²', round2(PI * r * h) + ' cm²', round2(csa + 10) + ' cm²']), exp: `CSA = 2πrh = 2 × 22/7 × ${r} × ${h} = ${fmt(round2(csa))} cm².` }; },
  () => { const r = pick([7, 14, 21]); const v = (4 / 3) * PI * r ** 3; return { wa: `⚽ Find the volume of a sphere of radius ${r} cm. (π = 22/7)`, o: opts(round2(v) + ' cm³', [round2(4 * PI * r * r) + ' cm³', round2(PI * r ** 3) + ' cm³', round2(v + 100) + ' cm³']), exp: `Volume = ⁴⁄₃πr³ = ⁴⁄₃ × 22/7 × ${fmt(r ** 3)} = ${fmt(round2(v))} cm³.` }; },
  () => { const r = pick([7, 14, 21]); const sa = 4 * PI * r * r; return { wa: `⚽ Find the surface area of a sphere of radius ${r} cm. (π = 22/7)`, o: opts(round2(sa) + ' cm²', [round2(PI * r * r) + ' cm²', round2(2 * PI * r * r) + ' cm²', round2(sa + 20) + ' cm²']), exp: `Surface area = 4πr² = 4 × 22/7 × ${r * r} = ${fmt(round2(sa))} cm².` }; },
  () => { const r = pick([7, 14, 21]), h = ri(5, 20); const v = (1 / 3) * PI * r * r * h; return { wa: `🍦 Find the volume of a cone of radius ${r} cm and height ${h} cm. (π = 22/7)`, o: opts(round2(v) + ' cm³', [round2(3 * v) + ' cm³', round2(PI * r * h) + ' cm³', round2(v + 30) + ' cm³']), exp: `Volume = ⅓πr²h = ⅓ × 22/7 × ${r * r} × ${h} = ${fmt(round2(v))} cm³.` }; },
  () => { const s = ri(3, 12); return { wa: `🧊 If the edge of a cube is doubled, how does its volume change?`, o: opts('It becomes 8 times bigger', ['It doubles', 'It becomes 4 times bigger', 'It stays the same']), exp: `Volume goes as edge³. Doubling the edge gives 2³ = 8 times the volume.` }; },
  () => { const l = ri(20, 60), b = ri(10, 40), h = ri(5, 20); const litres = round2((l * b * h) / 1000); return { wa: `🪣 A tank is ${l} cm × ${b} cm × ${h} cm. How many litres does it hold? (1000 cm³ = 1 litre)`, o: opts(litres + ' litres', [l * b * h + ' litres', round2(litres * 10) + ' litres', round2(litres + 2) + ' litres']), exp: `Volume = ${fmt(l * b * h)} cm³ = ${fmt(l * b * h)} ÷ 1000 = ${litres} litres.` }; },
];

/* ==================================================== coordinate geometry === */
const coordinates = [
  () => { const x1 = ri(-9, 9), y1 = ri(-9, 9), x2 = ri(-9, 9), y2 = ri(-9, 9); return { wa: `📍 Find the midpoint of (${x1}, ${y1}) and (${x2}, ${y2}).`, o: among(`(${round2((x1 + x2) / 2)}, ${round2((y1 + y2) / 2)})`, [`(${x1 + x2}, ${y1 + y2})`, `(${round2((x1 - x2) / 2)}, ${round2((y1 - y2) / 2)})`, `(${round2((y1 + y2) / 2)}, ${round2((x1 + x2) / 2)})`]), exp: `Midpoint = ((${x1}+${x2})/2, (${y1}+${y2})/2) = (${round2((x1 + x2) / 2)}, ${round2((y1 + y2) / 2)}).` }; },
  () => { const t = pick([[3, 4, 5], [6, 8, 10], [5, 12, 13], [8, 15, 17]]); return { wa: `📏 Find the distance of the point (${t[0]}, ${t[1]}) from the origin.`, o: opts(t[2], near(t[2])), exp: `Distance = √(${t[0]}² + ${t[1]}²) = √${t[0] ** 2 + t[1] ** 2} = ${t[2]}.` }; },
  () => { const t = pick([[3, 4, 5], [6, 8, 10], [5, 12, 13]]); const x = ri(-5, 5), y = ri(-5, 5); return { wa: `📏 Find the distance between (${x}, ${y}) and (${x + t[0]}, ${y + t[1]}).`, o: opts(t[2], near(t[2])), exp: `The gaps are ${t[0]} and ${t[1]}, so distance = √(${t[0] ** 2} + ${t[1] ** 2}) = √${t[0] ** 2 + t[1] ** 2} = ${t[2]}.` }; },
  () => { const x = ri(1, 9), y = ri(1, 9); const q = pick([[x, y, 'First'], [-x, y, 'Second'], [-x, -y, 'Third'], [x, -y, 'Fourth']]); return { wa: `🧭 In which quadrant does the point (${q[0]}, ${q[1]}) lie?`, o: among(q[2], ['First', 'Second', 'Third', 'Fourth'].filter((z) => z !== q[2])), exp: `x is ${q[0] > 0 ? 'positive' : 'negative'} and y is ${q[1] > 0 ? 'positive' : 'negative'}, which is the ${q[2].toLowerCase()} quadrant.` }; },
  () => { const x = ri(-9, 9); return { wa: `📍 On which axis does the point (${x}, 0) lie?`, o: among('The x-axis', ['The y-axis', 'The origin', 'No axis']), exp: `The y-coordinate is 0, so the point sits on the x-axis.` }; },
  () => { const y = ri(-9, 9); return { wa: `📍 On which axis does the point (0, ${y}) lie?`, o: among('The y-axis', ['The x-axis', 'The origin', 'No axis']), exp: `The x-coordinate is 0, so the point sits on the y-axis.` }; },
  () => { const x = ri(1, 9), y = ri(1, 9); return { wa: `🪞 Find the image of (${x}, ${y}) after reflection in the x-axis.`, o: among(`(${x}, ${-y})`, [`(${-x}, ${y})`, `(${-x}, ${-y})`, `(${y}, ${x})`]), exp: `Reflecting in the x-axis keeps x and flips the sign of y: (${x}, ${-y}).` }; },
  () => { const x = ri(1, 9), y = ri(1, 9); return { wa: `🪞 Find the image of (${x}, ${y}) after reflection in the y-axis.`, o: among(`(${-x}, ${y})`, [`(${x}, ${-y})`, `(${-x}, ${-y})`, `(${y}, ${x})`]), exp: `Reflecting in the y-axis flips the sign of x and keeps y: (${-x}, ${y}).` }; },
  () => { const x = ri(1, 9), y = ri(1, 9); return { wa: `🪞 Find the image of (${x}, ${y}) after reflection in the origin.`, o: among(`(${-x}, ${-y})`, [`(${x}, ${-y})`, `(${-x}, ${y})`, `(${y}, ${x})`]), exp: `Reflecting in the origin flips both signs: (${-x}, ${-y}).` }; },
  () => { const x1 = ri(-8, 8), y1 = ri(-8, 8), dx = ri(1, 6), dy = ri(1, 6); const m = round2(dy / dx); return { wa: `📈 Find the slope of the line joining (${x1}, ${y1}) and (${x1 + dx}, ${y1 + dy}).`, o: opts(m, [round2(dx / dy), round2(-dy / dx), round2(m + 1)]), exp: `Slope = (change in y) ÷ (change in x) = ${dy} ÷ ${dx} = ${m}.` }; },
  () => { const x1 = ri(-6, 6), y1 = ri(-6, 6), x2 = ri(-6, 6), y2 = ri(-6, 6); const [ax, ay] = [x1 + x2, y1 + y2]; return { wa: `📐 The midpoint of (${x1}, ${y1}) and (x, y) is (${round2(ax / 2)}, ${round2(ay / 2)}). What is (x, y)?`, o: among(`(${x2}, ${y2})`, [`(${ax}, ${ay})`, `(${x1}, ${y1})`, `(${-x2}, ${-y2})`]), exp: `x = 2×${round2(ax / 2)} − ${x1} = ${x2}, and y = 2×${round2(ay / 2)} − ${y1} = ${y2}.` }; },
  () => { return { wa: `🧭 What are the coordinates of the origin?`, o: among('(0, 0)', ['(1, 1)', '(0, 1)', '(1, 0)']), exp: `The origin is where the two axes cross, at (0, 0).` }; },
];

/* ========================================================== trigonometry === */
const trigonometry = [
  () => { const t = pick([['sin', 30, '1/2'], ['sin', 90, '1'], ['sin', 0, '0'], ['cos', 60, '1/2'], ['cos', 0, '1'], ['cos', 90, '0'], ['tan', 45, '1'], ['tan', 0, '0'], ['sin', 45, '1/√2'], ['cos', 45, '1/√2'], ['sin', 60, '√3/2'], ['cos', 30, '√3/2']]); return { wa: `📐 What is ${t[0]} ${t[1]}°?`, o: among(t[2], ['0', '1', '1/2', '1/√2', '√3/2', '√3'].filter((x) => x !== t[2]).slice(0, 3)), exp: `${t[0]} ${t[1]}° = ${t[2]} — one of the standard values worth memorising.` }; },
  () => { const t = pick([[3, 4, 5], [6, 8, 10], [5, 12, 13], [8, 15, 17]]); return { wa: `📐 In a right triangle the side opposite angle θ is ${t[0]} and the hypotenuse is ${t[2]}. Find sin θ.`, o: among(`${t[0]}/${t[2]}`, [`${t[1]}/${t[2]}`, `${t[0]}/${t[1]}`, `${t[2]}/${t[0]}`]), exp: `sin θ = opposite ÷ hypotenuse = ${t[0]}/${t[2]}.` }; },
  () => { const t = pick([[3, 4, 5], [6, 8, 10], [5, 12, 13]]); return { wa: `📐 In a right triangle the side adjacent to θ is ${t[1]} and the hypotenuse is ${t[2]}. Find cos θ.`, o: among(`${t[1]}/${t[2]}`, [`${t[0]}/${t[2]}`, `${t[1]}/${t[0]}`, `${t[2]}/${t[1]}`]), exp: `cos θ = adjacent ÷ hypotenuse = ${t[1]}/${t[2]}.` }; },
  () => { const t = pick([[3, 4, 5], [6, 8, 10], [5, 12, 13]]); return { wa: `📐 In a right triangle the side opposite θ is ${t[0]} and the adjacent side is ${t[1]}. Find tan θ.`, o: among(`${t[0]}/${t[1]}`, [`${t[1]}/${t[0]}`, `${t[0]}/${t[2]}`, `${t[2]}/${t[1]}`]), exp: `tan θ = opposite ÷ adjacent = ${t[0]}/${t[1]}.` }; },
  () => { return { wa: `🔁 What does sin²θ + cos²θ equal?`, o: opts('1', ['0', '2', 'tan θ']), exp: `This is the fundamental identity: sin²θ + cos²θ = 1 for every angle θ.` }; },
  () => { const a = ri(10, 80); return { wa: `🔄 sin ${a}° is equal to cos of which angle?`, o: opts(90 - a + '°', [a + '°', 180 - a + '°', 90 + a + '°']), exp: `sin θ = cos(90° − θ), so sin ${a}° = cos ${90 - a}°.` }; },
  () => { const h = ri(5, 40); return { wa: `🗼 A tower is ${h} m tall and its shadow is ${h} m long. What is the sun's angle of elevation?`, o: opts('45°', ['30°', '60°', '90°']), exp: `tan θ = height ÷ shadow = ${h}/${h} = 1, and tan 45° = 1, so θ = 45°.` }; },
  () => { const h = ri(2, 20); return { wa: `🪜 A ladder leans against a wall making 60° with the ground. If it reaches ${h} m up the wall, is the ladder longer or shorter than ${h} m?`, o: among('Longer than ' + h + ' m', ['Shorter than ' + h + ' m', 'Exactly ' + h + ' m', 'Cannot be decided']), exp: `The ladder is the hypotenuse, and a hypotenuse is always the longest side — so it is longer than ${h} m.` }; },
  () => { return { wa: `📐 What is tan θ in terms of sin θ and cos θ?`, o: among('sin θ / cos θ', ['cos θ / sin θ', 'sin θ × cos θ', 'sin θ + cos θ']), exp: `tan θ = opposite/adjacent, which is the same as sin θ ÷ cos θ.` }; },
  () => { return { wa: `📏 What is the angle of elevation when you look at something at your own eye level?`, o: opts('0°', ['45°', '90°', '180°']), exp: `There is no lifting of the line of sight, so the angle of elevation is 0°.` }; },
];

/* ============================================================= statistics === */
const statistics = [
  () => { const v = Array.from({ length: 5 }, () => ri(2, 40)); const s = v.reduce((a, b) => a + b, 0); return { wa: `📊 Find the mean of ${v.join(', ')}.`, o: opts(round2(s / 5), [s, round2(s / 4), round2(s / 5 + 2)]), exp: `Mean = (${v.join(' + ')}) ÷ 5 = ${s} ÷ 5 = ${round2(s / 5)}.` }; },
  () => { const v = Array.from({ length: 5 }, () => ri(2, 40)); const sorted = [...v].sort((a, b) => a - b); return { wa: `📊 Find the median of ${v.join(', ')}.`, o: opts(sorted[2], [v[0], round2(v.reduce((a, b) => a + b, 0) / 5), sorted[3]]), exp: `In order: ${sorted.join(', ')}. The middle value is ${sorted[2]}.` }; },
  () => { const r = ri(2, 20); const v = [r, r, ...Array.from({ length: 3 }, () => ri(21, 40))]; return { wa: `📊 Find the mode of ${H.shuffle(v).join(', ')}.`, o: opts(r, [v[2], v[3], round2(v.reduce((a, b) => a + b, 0) / 5)]), exp: `${r} appears twice while every other value appears once, so the mode is ${r}.` }; },
  () => { const v = Array.from({ length: 6 }, () => ri(2, 40)); const sorted = [...v].sort((a, b) => a - b); const med = round2((sorted[2] + sorted[3]) / 2); return { wa: `📊 Find the median of ${v.join(', ')}.`, o: opts(med, [sorted[2], sorted[3], round2(med + 2)]), exp: `In order: ${sorted.join(', ')}. With six values the median is the average of the middle two: (${sorted[2]} + ${sorted[3]}) ÷ 2 = ${med}.` }; },
  () => { const v = Array.from({ length: 6 }, () => ri(2, 60)); return { wa: `📏 Find the range of ${v.join(', ')}.`, o: opts(Math.max(...v) - Math.min(...v), [Math.max(...v), Math.min(...v), Math.max(...v) + Math.min(...v)]), exp: `Range = largest − smallest = ${Math.max(...v)} − ${Math.min(...v)} = ${Math.max(...v) - Math.min(...v)}.` }; },
  () => { const n = ri(4, 10), mean = ri(10, 50); return { wa: `🧮 The mean of ${n} numbers is ${mean}. What is their total?`, o: opts(n * mean, [mean, round2(mean / n), n * mean + n]), exp: `Total = mean × how many = ${mean} × ${n} = ${fmt(n * mean)}.` }; },
  () => { const n = ri(4, 9), mean = ri(10, 40), extra = ri(10, 60); const newMean = round2((n * mean + extra) / (n + 1)); return { wa: `➕ The mean of ${n} numbers is ${mean}. One more number, ${extra}, is added. What is the new mean?`, o: opts(newMean, [mean, round2(mean + extra), round2(newMean + 2)]), exp: `Old total = ${n * mean}. New total = ${n * mean + extra} over ${n + 1} numbers = ${newMean}.` }; },
  () => { return { wa: `🏷️ Which average is the value that appears most often?`, o: among('Mode', ['Mean', 'Median', 'Range']), exp: `The mode is the most frequently occurring value.` }; },
  () => { return { wa: `🏷️ Which average is the middle value when data is arranged in order?`, o: among('Median', ['Mean', 'Mode', 'Range']), exp: `The median is the middle value once the data is sorted.` }; },
  () => { const v = [ri(2, 20), ri(2, 20), ri(2, 20)]; const s = v.reduce((a, b) => a + b, 0); const target = ri(20, 40); const need = target * 4 - s; return { wa: `🎯 ${pick(NAMES)} scored ${v.join(', ')} in three tests. What is needed in the fourth to average ${target}?`, o: opts(need, near(need)), exp: `Four tests averaging ${target} need a total of ${target * 4}. So far: ${s}. Needed: ${target * 4} − ${s} = ${need}.` }; },
];

/* ============================================================ probability === */
const probability = [
  () => { const t = ri(2, 6); return { wa: `🎲 A fair die is rolled. What is the probability of getting a ${t}?`, o: among('1/6', ['1/2', '1/3', `${t}/6`]), exp: `There are 6 equally likely faces and one is a ${t}, so the probability is 1/6.` }; },
  () => { return { wa: `🎲 A fair die is rolled. What is the probability of getting an even number?`, o: among('1/2', ['1/3', '1/6', '2/3']), exp: `The even faces are 2, 4 and 6 — that is 3 out of 6, which is 1/2.` }; },
  () => { return { wa: `🪙 A fair coin is tossed. What is the probability of getting a head?`, o: among('1/2', ['1', '1/4', '0']), exp: `There are two equally likely outcomes and one is a head, so 1/2.` }; },
  () => { const r = ri(2, 8), b = ri(2, 8); return { wa: `🎒 A bag holds ${r} red and ${b} blue balls. One is drawn. What is the probability it is red?`, o: among(`${r}/${r + b}`, [`${b}/${r + b}`, `${r}/${b}`, `1/${r + b}`]), exp: `${r} red out of ${r + b} balls in all, so the probability is ${r}/${r + b}.` }; },
  () => { const r = ri(2, 8), b = ri(2, 8); return { wa: `🎒 A bag holds ${r} red and ${b} blue balls. What is the probability of NOT drawing red?`, o: among(`${b}/${r + b}`, [`${r}/${r + b}`, `1/${r + b}`, `${r}/${b}`]), exp: `Not red means blue: ${b} out of ${r + b}, so ${b}/${r + b}.` }; },
  () => { return { wa: `🃏 A card is drawn from a standard pack of 52. What is the probability it is a heart?`, o: among('1/4', ['1/13', '1/52', '1/2']), exp: `There are 13 hearts in 52 cards: 13/52 = 1/4.` }; },
  () => { return { wa: `🃏 A card is drawn from a pack of 52. What is the probability it is an ace?`, o: among('1/13', ['1/4', '1/52', '4/13']), exp: `There are 4 aces in 52 cards: 4/52 = 1/13.` }; },
  () => { const p = ri(1, 9); return { wa: `➖ The probability of an event is ${p}/10. What is the probability it does NOT happen?`, o: among(`${10 - p}/10`, [`${p}/10`, `1/10`, `${p}/${10 - p}`]), exp: `The two probabilities add to 1: 1 − ${p}/10 = ${10 - p}/10.` }; },
  () => { return { wa: `🎯 What is the probability of an event that is certain to happen?`, o: opts('1', ['0', '1/2', '100']), exp: `A certain event has probability 1.` }; },
  () => { return { wa: `🚫 What is the probability of an impossible event?`, o: opts('0', ['1', '1/2', '−1']), exp: `An impossible event has probability 0.` }; },
  () => { const n = ri(10, 40), f = ri(2, 9); return { wa: `📊 In ${n} trials an event happened ${f} times. What is its experimental probability?`, o: among(`${f}/${n}`, [`${n}/${f}`, `${f}/100`, `1/${n}`]), exp: `Experimental probability = times it happened ÷ total trials = ${f}/${n}.` }; },
  () => { return { wa: `🔢 Can the probability of an event be 1.5?`, o: yesNo(false, 'Yes', 'No — it must lie between 0 and 1'), exp: `Probability always lies between 0 and 1 inclusive, so 1.5 is impossible.` }; },
];

/* ============================================================ measurement === */
const measurement = [
  () => { const m = ri(2, 40); return { wa: `📏 Convert ${m} metres into centimetres.`, o: opts(m * 100 + ' cm', [m * 10 + ' cm', m * 1000 + ' cm', round2(m / 100) + ' cm']), exp: `1 m = 100 cm, so ${m} m = ${fmt(m * 100)} cm.` }; },
  () => { const k = ri(2, 30); return { wa: `📏 Convert ${k} kilometres into metres.`, o: opts(k * 1000 + ' m', [k * 100 + ' m', k * 10 + ' m', round2(k / 1000) + ' m']), exp: `1 km = 1000 m, so ${k} km = ${fmt(k * 1000)} m.` }; },
  () => { const kg = ri(2, 30); return { wa: `⚖️ Convert ${kg} kilograms into grams.`, o: opts(kg * 1000 + ' g', [kg * 100 + ' g', kg * 10 + ' g', round2(kg / 1000) + ' g']), exp: `1 kg = 1000 g, so ${kg} kg = ${fmt(kg * 1000)} g.` }; },
  () => { const l = ri(2, 30); return { wa: `🥛 Convert ${l} litres into millilitres.`, o: opts(l * 1000 + ' ml', [l * 100 + ' ml', l * 10 + ' ml', round2(l / 1000) + ' ml']), exp: `1 litre = 1000 ml, so ${l} litres = ${fmt(l * 1000)} ml.` }; },
  () => { const h = ri(1, 10); return { wa: `⏰ How many minutes are there in ${h} hour(s)?`, o: opts(h * 60, [h * 100, h * 24, h * 60 + 10]), exp: `1 hour = 60 minutes, so ${h} × 60 = ${h * 60} minutes.` }; },
  () => { const d = ri(2, 14); return { wa: `📅 How many hours are there in ${d} days?`, o: opts(d * 24, [d * 12, d * 60, d * 7]), exp: `1 day = 24 hours, so ${d} × 24 = ${d * 24} hours.` }; },
  () => { const sh = ri(1, 10), sm = pick([0, 15, 30, 45]), dur = ri(20, 120); const start = sh * 60 + sm, end = start + dur; return { wa: `🕰️ A class starts at ${String(sh).padStart(2, '0')}:${String(sm).padStart(2, '0')} and runs for ${dur} minutes. When does it end?`, o: among(`${String(Math.floor(end / 60)).padStart(2, '0')}:${String(end % 60).padStart(2, '0')}`, [`${String(Math.floor(end / 60)).padStart(2, '0')}:${String((end % 60) + 5).padStart(2, '0')}`, `${String(Math.floor(end / 60) + 1).padStart(2, '0')}:${String(end % 60).padStart(2, '0')}`, `${String(sh).padStart(2, '0')}:${String(sm).padStart(2, '0')}`]), exp: `${String(sh).padStart(2, '0')}:${String(sm).padStart(2, '0')} plus ${dur} minutes is ${String(Math.floor(end / 60)).padStart(2, '0')}:${String(end % 60).padStart(2, '0')}.` }; },
  () => { const cm = ri(2, 90) * 100; return { wa: `📏 Convert ${fmt(cm)} centimetres into metres.`, o: opts(cm / 100 + ' m', [cm * 100 + ' m', round2(cm / 1000) + ' m', round2(cm / 10) + ' m']), exp: `100 cm = 1 m, so ${fmt(cm)} ÷ 100 = ${cm / 100} m.` }; },
  () => { const g = ri(2, 40) * 1000; return { wa: `⚖️ Convert ${fmt(g)} grams into kilograms.`, o: opts(g / 1000 + ' kg', [g * 1000 + ' kg', round2(g / 100) + ' kg', round2(g / 10) + ' kg']), exp: `1000 g = 1 kg, so ${fmt(g)} ÷ 1000 = ${g / 1000} kg.` }; },
  () => { const y = pick([2016, 2020, 2024, 2028, 2019, 2021, 2023, 2025]); const leap = (y % 4 === 0 && y % 100 !== 0) || y % 400 === 0; return { wa: `📅 Is ${y} a leap year?`, o: yesNo(leap, 'Yes', 'No'), exp: `${y} ${leap ? 'is divisible by 4, so it is a leap year with 366 days' : 'is not divisible by 4, so it is a normal year with 365 days'}.` }; },
];

module.exports = { linesAngles, triangles, quadrilaterals, circles, perimeterArea,
  solids, coordinates, trigonometry, statistics, probability, measurement };
